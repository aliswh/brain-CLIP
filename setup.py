import setuptools

setuptools.setup(
   name='brainclip',
   version='1.0',
   description='brainclip',
   author='Alice Schiavone',
   author_email='alice.schiavone.97@gmail.com',
   packages=['brainclip'],  #same as name
   install_requires=['numpy'], #external packages as dependencies
)
